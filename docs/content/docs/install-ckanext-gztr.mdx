---
title: "Install ckanext-gztr"
description: Learn about how to install ckanext-gztr to your CKAN instancce.
icon: Blocks
---


# Installation and Setup

We assume you're familiar with setting up CKAN extensions.

In this installation guide we'll use a CKAN source install set up for development on Ubuntu 24.04 and we'll refer to the default installation location of `/usr/lib/ckan/default/src/ckan` during these steps along with the configuration file at `/etc/ckan/default/ckan.ini`. Our installation is based on [`ckan-devstaller`](https://ckan-devstaller.dathere.com).

<Callout title="Just trying out the extension?" type="info">If setting up a new CKAN instance for experimenting we suggest using [ckan-devstaller](https://ckan-devstaller.dathere.com).</Callout>

<Callout title="Where have we tried ckanext-gztr?" type="info">The ckanext-gztr extension has worked on CKAN 2.11.4 and hasn't been verified to work for other versions.</Callout>

import { Step, Steps } from 'fumadocs-ui/components/steps';

<Steps>
<Step>

## Activate your virtual environment and change directory to `/usr/lib/ckan/default/src`

Ensure your terminal is running in your instance's virtual environment before continuing and that you're in the `/usr/lib/ckan/default/src` directory so that we may install extensions here.

```bash
cd /usr/lib/ckan/default/src
. ../bin/activate
```

</Step>
<Step>

## Install ckanext-scheming

The ckanext-gztr extension relies on [ckanext-scheming](https://github.com/ckan/ckanext-scheming) to add the interactive gazetteer to the new dataset form.

Run the following in `/usr/lib/ckan/default/src` to install the ckanext-scheming extension:

```bash
pip install -e "git+https://github.com/ckan/ckanext-scheming.git#egg=ckanext-scheming"
```

Then in your CKAN config file (e.g., `/etc/ckan/default/ckan.ini`) for `ckan.plugins` add `scheming_datasets` and `scheming_organizations`. For example:

```ini
ckan.plugins = activity scheming_datasets scheming_organization
```

Also add the following lines in the `.ini` file (e.g. right below the line with `ckan.plugins`) for displaying the gazetteer in the dataset form:

```ini
# ckanext-scheming theme settings
scheming.presets = ckanext.scheming:presets.json ckanext.gztr:schemas/presets.yaml
scheming.dataset_schemas = ckanext.gztr:schemas/dataset.yaml ckanext.gztr:schemas/application.yaml
```

</Step>
<Step>

## Install ckanext-gztr

In `/usr/lib/ckan/default/src` (within an activated virtual environment) run the following:

```bash
git clone https://github.com/dathere/ckanext-gztr.git
cd ckanext-gztr
pip install -e .
pip install -r requirements.txt
```

Then add `gztr` to your `ckan.plugins` in your `.ini` file:

```ini
ckan.plugins = activity scheming_datasets scheming_organizations gztr
```

</Step>

<Step>

## Verify ckanext-gztr works as expected

Now the interactive gazetteer should be available on the form for adding and editing a dataset along with a "Spatial" field which is used to save the selected features along with a public search map gazetteer.

</Step>

<Step>

## Customize ckanext-gztr for your CKAN instance

You may want to customize various parts of ckanext-gztr. Click on one of the previews below to go to that feature's customization documentation.

</Step>

{/* <Step>

## Install ckanext-spatial

You may also add a public interactive map for searching through datasets by drawing a bounding box using the [ckanext-spatial](https://github.com/ckan/ckanext-spatial) extension.

We recommend viewing the [ckanext-spatial documentation](https://docs.ckan.org/projects/ckanext-spatial/en/latest) for the latest installation instructions. We have included an example of steps that worked for us below.

First install required dependencies (may differ based on your platform):

```bash
sudo apt-get install python-dev libxml2-dev libxslt1-dev libgeos-c1
```

Then run the following in `/usr/lib/ckan/default/src`:

```bash
git clone https://github.com/dathere/ckanext-spatial.git
cd ckanext-spatial
git switch gztr
pip install -e .
pip install -r requirements.txt
```

Then add the `spatial_metadata` and `spatial_query` plugins to `ckan.plugins` in your `.ini` file, for example:

```ini
ckan.plugins = activity scheming_datasets scheming_organizations spatial_metadata spatial_query gztr
```

Next you'll want to add the relevant config to your CKAN config file. Here's an example:

```ini
ckanext.spatial.search_backend = solr-spatial-field
ckanext.spatial.common_map.type = custom
ckanext.spatial.common_map.custom_url = https://tile.openstreetmap.org/{z}/{x}/{y}.png
ckanext.spatial.common_map.attribution = Map data from <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>
```

You'll also need to modify you SOLR schema as per the steps [mentioned](https://docs.ckan.org/projects/ckanext-spatial/en/latest/spatial-search/#choosing-a-backend-for-the-spatial-search) in the ckanext-spatial docs when choosing a spatial search backend. For example if you're using [ckan-compose](https://github.com/tino097/ckan-compose/blob/ckan-devstaller/solr/schema.xml) as your backend for CKAN installed from your home directory then you can modify `ckan-compose/solr/schema.xml`. As an example:

```xml
    <types>
        <!-- ... -->
        <fieldType name="location_rpt"   class="solr.SpatialRecursivePrefixTreeFieldType"
            spatialContextFactory="JTS"
            autoIndex="true"
            validationRule="repairBuffer0"
            distErrPct="0.025"
            maxDistErr="0.001"
            distanceUnits="kilometers" />
    </types>

    <fields>
        <!-- ... -->
        <field name="spatial" type="location_rpt" indexed="true" multiValued="true" />
    </fields>
```

Notice that instead of `spatial_geom` as mentioned in the docs for the field name we use `spatial`.

{/* TODO: Install JTS as per https://github.com/tino097/ckan-compose/blob/twdh-schema/solr/Dockerfile or similar to fix java NoClassDefinedError
TODO: Update location of this user agent code or add to ckan.ini /*}

</Step> */}
</Steps>
